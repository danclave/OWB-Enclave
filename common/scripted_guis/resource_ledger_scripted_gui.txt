scripted_gui = {
	resource_ledger = {
		context_type = player_context

		window_name = "resource_ledger"

		visible = {
			is_ai = no
			check_variable = { show_resource_ledger > 0 }
		}

		effects = {
			close_button_click = {
				clear_variable = show_resource_ledger
			}
			entry_flag_click = {
				capital_scope = {
					ROOT = {
						goto_state = PREV
					}
				}
			}
		}

		triggers = {
			harvest_resources_visible = {
				harvest_resources_visible_trigger = yes
			}

			harvest_resources_click_enabled = {
				harvest_resources_click_enabled_trigger = yes
			}

			harvest_resources_recall_visible = {
				harvest_resources_recall_visible_trigger = yes
			}

			harvest_resources_recall_click_enabled = {
				
			}

			steal_military_industry_visible = {
				steal_industry_visible = yes
			}

            steal_military_industry_click_enabled = {
				steal_military_industry_click_enabled_trigger = yes
            }

			steal_civilian_industry_visible = {
				steal_industry_visible = yes
			}

			steal_civilian_industry_click_enabled = {
				steal_civilian_industry_click_enabled_trigger = yes
            }

			establish_outpost_visible = {
				establish_outpost_visible_trigger = yes
			}

			establish_outpost_click_enabled = {
				establish_outpost_click_enabled_trigger = yes
            }

			abandon_outpost_visible = {
				abandon_outpost_visible_trigger = yes
			}

           	abandon_outpost_click_enabled = {
			
        	}
		}


		dynamic_lists = {
			resource_ledger_gridbox = {
				array = ERB.state_list
				#array = subjects
				entry_container = resource_ledger_entry
				change_scope = yes
			}
		}
		
		#properties = {
		#	entry_flag = {
		#		image = "[This.GetFlag]"
		#	}
		#	conscription_law_icon = {
		#		image = "[This.GetConscriptionLawIcon]"
		#	}
		#}
	}
}
